import styled from '@emotion/styled'

import {
  space,
  width,
  maxWidth,
  height,
  textAlign,
  color,
  background
} from 'styled-system'

const Box = styled.div`
  ${space}
  ${width}
  ${maxWidth}
  ${height}
  ${textAlign}
  ${color}
  ${background};
`

export default Box
