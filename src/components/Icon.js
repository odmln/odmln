import styled from '@emotion/styled'

import {
  color,
  space,
  width,
  height
} from 'styled-system'

const Icon = styled.img`
  display: block;
  ${color}
  ${space}
  ${width}
  ${height};
`

export default Icon
